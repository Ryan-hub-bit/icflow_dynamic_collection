# Load the current Arch Linux defaults, then keep the LLVM labels in produced
# binaries and make sure PKGBUILD check() functions are enabled.
source /etc/makepkg.conf

OPTIONS=(!strip)
BUILDENV=(!distcc color !ccache check !sign)
